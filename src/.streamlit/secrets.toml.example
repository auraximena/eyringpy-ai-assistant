# Copia este archivo como secrets.toml y coloca tu clave real.
# secrets.toml NUNCA debe subirse al repositorio (ya está en .gitignore).
# Alternativa: usar el archivo .env en la raíz del proyecto.

GOOGLE_API_KEY = "tu_clave_de_gemini_aqui"
